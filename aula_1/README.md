# Aula 1

Projeto inicial com um servidor Express simples em TypeScript.

## Estrutura

- `src/server.ts`: servidor Express com rota `GET /`.
- `package.json`: script de desenvolvimento usando `ts-node/esm`.

## Como executar

```bash
npm install
npm run dev
```

A aplicação ficará disponível em `http://localhost:3333`.
